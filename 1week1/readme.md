# Practice 1: Git, GitHub, and Python Basics

This repository contains the completed tasks for Practice 1, focusing on setting up the development environment, mastering version control basics, and learning introductory Python syntax.

## Completed Tasks

1. **Environment Setup & Git Configuration:**
   * Installed Git locally and configured global user settings (`user.name` and `user.email`).
   * Created a GitHub account and initialized this remote repository.

2. **Git Foundations (GitHowTo):**
   * Completed the first 13 topics on *GitHowTo*, covering the staging area, committing changes, viewing logs, undoing local/staged changes, and tagging.
   * Practiced version control workflows directly inside Emacs using Magit/Shell.

3. **Python Basics (W3Schools):**
   * Reviewed W3Schools Python tutorials from *HOME* through *Python Strings*.
   * Created 5 distinct script files implementing key concepts:
     * `hello_world.py` – Syntax and comments.
     * `variables.py` – Variables assigning and type casting.
     * `data_types.py` – Built-in data types and checking with `type()`.
     * `numbers_test.py` – Numeric types (int, float, complex) and the `random` module.
     * `strings_demo.py` – Slicing, modifying, concatenating, and formatting f-strings.

## How to Run the Code

To execute any of the Python scripts, run the following command in your terminal:

```bash
python 1week1/name_of_file.py
